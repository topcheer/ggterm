name: ggterm_mobile
description: GGTerm mobile terminal emulator (Flutter UI shell).
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.0.0
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
